import 'package:flutter/material.dart';



class registroList extends StatelessWidget {
  const registroList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Listado', style: TextStyle(fontSize: 30),),
    );
  }
}